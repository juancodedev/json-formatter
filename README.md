# JSON Formatter

Simple web app to validate, format, and minify JSON.

## Run locally

Open `index.html` in your browser.

## Features

- Format JSON with 2-space indentation
- Minify JSON
- Validation feedback for invalid JSON
